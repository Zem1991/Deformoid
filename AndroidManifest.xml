<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="deformoid.mecanica"
    android:versionCode="2"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.D1" >
            <intent-filter>
                <action android:name="com.deformoid.mecanica.MAINACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Equipe"
            android:theme="@style/Theme.D1" >
        </activity>
        <activity
            android:name=".GraficoActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Gráfico" >
        </activity>
        <activity
            android:name=".StartDrawActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Deformoid" >
            <intent-filter>
                <action android:name="com.deformoid.mecanica.STARTDRAWACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Splash"
            android:configChanges="orientation|keyboardHidden"
            android:label="Deformoid" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>